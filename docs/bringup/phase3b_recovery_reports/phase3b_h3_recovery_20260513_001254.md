# Phase 3B-H3 Recovery Report

## Phase

Phase 3B-H3 Sidecar Marker Load Proof

## Failed Or Deferred Command

```bash
env SKIP_DDR_INIT=1 JLINK_HOST=127.0.0.1 JLINK_PORT=3333 KERNEL_RUN_SECS=60 RUN_TAG=phase3b_h3_baseline_check bash ./run_phase2_ceva_bt_linux.sh
```

## Failure Symptom

The sidecar marker probe completed successfully, but the follow-up Linux baseline run stayed in the existing heavy Phase 2 launch flow long enough to exceed the interactive execution window. The log reached payload load / evidence clear stages but did not produce a final Linux baseline verdict.

## Files Touched

- `scripts/linux_boot_sidecar_marker_probe.gdb`
- `docs/bringup/ceva_bt52_phase3b_h3_sidecar_marker_load_proof_20260512.md`
- `docs/bringup/phase3b_recovery_reports/phase3b_h3_recovery_20260513_001254.md`

Generated evidence files were kept in `/tmp` and are not commit material.

## Recovery Strategy

1. Treat the Linux baseline run as deferred, not PASS.
2. Clean lingering GDB / launch processes.
3. Preserve the marker proof evidence as a valid H3 partial result.
4. Continue with H4 address and packaging preparation, which does not depend on a completed Linux boot baseline.

## Attempted Fixes

- Checked active processes after the terminal was killed.
- Confirmed no persistent run/GDB processes remained after cleanup.
- Confirmed H3 marker proof output contained `H3_MARKER_PROOF=PASS`.

## Final State

PARTIAL: Sidecar marker load and execution proof passed. Linux baseline non-regression was not completed and is not claimed.

## Why This Is Not Claimed As Real PASS

This proof does not enter CEVA vendor runtime, does not call `rwip_init()`, does not consume a real HCI command, and does not return a real HCI event. The interrupted Linux baseline also means H3 does not claim complete Linux non-regression.

## Next Safe Action

Proceed to H4 reserved memory / packaging freeze and define a faster future non-regression check. Do not enter H5 ingress bridge until H4 freezes the sidecar memory contract.
